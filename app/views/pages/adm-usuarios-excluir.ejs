<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inativar Usuário - Painel Administrativo</title>
    <link rel="stylesheet" href="/css/styleAdm.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
    <link rel="stylesheet" href="/css/admin-excluir.css">
</head>
<body>
    <section class="navbar navbar-inverse">
        <section class="navbar-header">
            <ul class="nav navbar-nav">
                <li><a href="/adm">Dashboard</a></li>
                <li class="active"><a href="/adm/usuarios">Usuários</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li><a href="/">Voltar ao Site</a></li>
            </ul>
        </section>
    </section>

    <section class="container">
        <section class="page-header">
            <h2>
                <span class="material-symbols-outlined">block</span>
                Inativar Usuário
            </h2>
            <p class="page-subtitle">Confirme a inativação do usuário no sistema</p>
        </section>

        <section class="alert alert-warning">
            <span class="material-symbols-outlined alert-icon">warning</span>
            <div class="alert-content">
                <strong>Atenção!</strong>
                <p>Você está prestes a <strong>INATIVAR</strong> este usuário. Os dados <strong>NÃO serão deletados</strong> do banco de dados, apenas marcados como inativos e o usuário não poderá mais acessar a plataforma.</p>
            </div>
        </section>

        <% if (usuario) { %>
            <section class="confirmation-card">
                <section class="card-header">
                    <h3>
                        <span class="material-symbols-outlined">person_remove</span>
                        Confirme os dados do usuário
                    </h3>
                </section>

                <section class="card-body">
                    <section class="user-preview">
                        <section class="user-avatar-large">
                            <span class="material-symbols-outlined">account_circle</span>
                        </section>
                        <section class="user-info-preview">
                            <dl class="info-list-preview">
                                <div class="info-item">
                                    <dt>Nome Completo</dt>
                                    <dd><%= usuario.NomeCompleto %></dd>
                                </div>
                                <div class="info-item">
                                    <dt>Email</dt>
                                    <dd><%= usuario.Email %></dd>
                                </div>
                                <div class="info-item">
                                    <dt>Telefone</dt>
                                    <dd>
                                        <%= usuario.Telefone ? 
                                            `(${usuario.Telefone.substring(0,2)}) ${usuario.Telefone.substring(2,7)}-${usuario.Telefone.substring(7)}` 
                                            : 'Não informado' %>
                                    </dd>
                                </div>
                                <div class="info-item">
                                    <dt>Tipo de Usuário</dt>
                                    <dd>
                                        <span class="badge badge-<%= usuario.UsuarioTipo === 'N' ? 'success' : 'info' %>">
                                            <%= usuario.TipoUsuarioNome %>
                                        </span>
                                    </dd>
                                </div>
                                <div class="info-item">
                                    <dt>Status Atual</dt>
                                    <dd>
                                        <span class="status-badge status-<%= usuario.UsuarioStatus === 1 ? 'ativo' : 'inativo' %>">
                                            <%= usuario.StatusNome %>
                                        </span>
                                    </dd>
                                </div>
                            </dl>
                        </section>
                    </section>

                    <section class="confirmation-message">
                        <span class="material-symbols-outlined">help</span>
                        <h4>Tem certeza que deseja inativar este usuário?</h4>
                        <p>Esta ação pode ser revertida posteriormente editando o status do usuário.</p>
                    </section>
                </section>

                <section class="card-footer">
                    <form action="/adm/excluir-usuario?id=<%= usuario.id %>" method="post" class="confirmation-form">
                        <button type="submit" class="btn btn-danger btn-lg">
                            <span class="material-symbols-outlined">block</span>
                            Sim, Inativar Usuário
                        </button>
                        <a href="/adm/usuarios" class="btn btn-default btn-lg">
                            <span class="material-symbols-outlined">close</span>
                            Não, Cancelar
                        </a>
                    </form>
                </section>
            </section>
        <% } else { %>
            <section class="alert alert-danger">
                <span class="material-symbols-outlined alert-icon">error</span>
                <div class="alert-content">
                    <strong>Erro!</strong>
                    <p>Usuário não encontrado no sistema.</p>
                </div>
            </section>
            <a href="/adm/usuarios" class="btn btn-primary">
                <span class="material-symbols-outlined">arrow_back</span>
                Voltar para Lista de Usuários
            </a>
        <% } %>

        <footer>
            <p>&copy; 2025 Sistema NutriWeb | Painel Administrativo</p>
        </footer>
    </section>
</body>
</html>